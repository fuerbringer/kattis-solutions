#include <cstdint>
#include <iostream>
#include <string>

int main(const int argCount, const char* args[]) {
  std::string numCasesStr;
  std::getline(std::cin, numCasesStr);
  const std::size_t numCases= std::atoi(numCases);

  for(std::size_t i = 0; i < numCases; i++) {
    std::uint8_t x = 0, y = 0;
  }

  return EXIT_SUCCESS;
}
